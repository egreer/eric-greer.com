#theme: jekyll-theme-slate
defaults:
  - scope:
      path: "assets/images"
    values:
      image: true
whitelist:
  - jekyll-redirect-from
gems:
  - jekyll-github-metadata
  - jekyll-mentions
  - jekyll-redirect-from
  - jekyll-sitemap
